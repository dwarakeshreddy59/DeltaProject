# .env.example – Copy this file to .env and fill in your PostgreSQL credentials.
# The application loads these values automatically at startup.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=pdf_portal
DB_USER=postgres
DB_PASSWORD=your_password_here

SECRET_KEY=change-me-in-production
